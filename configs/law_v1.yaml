# Primary config. Qwen3-14B replaced Ministral-3-14B-Reasoning on 2026-08-06:
# Ministral OOMs on T4 at any sequence length (seq-invariant 1.25 GiB fp16
# lm_head copy in unsloth's eager fused-CE fallback + untied embeddings) -
# see docs/ministral-t4-disqualification.md. Ministral's config is archived
# as configs/law_v1_ministral.yaml.
model:
  repo: unsloth/Qwen3-14B-unsloth-bnb-4bit
  revision: 46105e245750aad3be7fd1d81c21cb03a0e438ed
  # Qwen3 ChatML markers (unsloth's documented train_on_responses_only pair).
  instruction_part: "<|im_start|>user\n"
  response_part: "<|im_start|>assistant\n"

data:
  think_open: '<think>'
  think_close: '</think>'

lora:
  r: 32
  alpha: 32
  dropout: 0.0
  # Text-only model - no vision tower, plain module list is safe.
  target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]

train:
  seed: 3407
  lr: 2.0e-4
  warmup_ratio: 0.03
  weight_decay: 0.001
  optim: adamw_8bit
  lr_scheduler_type: linear
  smoke:
    max_seq_length: 2048
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 16
    max_steps: 60
    save_steps: 25
    dataset: data/smoke_v1.jsonl

hub:
  # Private; auto-created on first push (hub_private_repo). Single-GPU lane -
  # the DDP lane pushes elsewhere (see law_v1_ddp.yaml). Never share one ckpt
  # repo across base models or lanes: --resume would cross-load.
  checkpoint_repo: tantan01/tuned-law-v1-qwen-ckpt
